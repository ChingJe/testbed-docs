# Operations

本目錄只保存 Infrastructure source repository 一般操作文件沒有涵蓋的實驗室／機器特定資訊，
例如 site network、VirtualBox placement、Host GPU、local storage、migration 與可恢復程序。

與程式碼版本直接綁定的 commands、configuration reference 與標準 lifecycle 應留在 source
repository，不在此建立第二份可能過時的操作手冊。
